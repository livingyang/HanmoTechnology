chore(规范): 清理 homepage 残留 + 升级 validate 为 err 模式

- products.json: 删除 HanmoArcomage / HanmoWesnoth 的 homepage 字段
- docs/demos/<3>/manifest.json: 删除 3 处 homepage 字段
- docs/index.html + assets: 重建反映 products.json 变更
- tools/validate-manifests.mjs: forbidden 字段从 warn 升级为 err
  - 反证测试：临时注入 homepage，validate 立即 exit 1
  - 同步清理 stale JS bundle

主线目的：执行"主页禁源码"规则，对齐 manifest v3.0 schema（DEMO-HOSTING.md §3.3）。
之前 forbid 仅 warning，下次 sync 必然再带 homepage 进来；改为 err 后强制人工清理。
