# Logs
logs
*.log
npm-debug.log*

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules
jspm_packages

# Hub 仓临时暂存：AI 在产品仓跑完 §9.6.1 后 cp 到此处，
# 用户跑 tools/publish-demo-release.ps1 上传 release 完即可删除 zip。
# 只忽略 zip 本身，README.md + .gitkeep 仍可入库。
temp/**/*.zip

# temp/<slug>/<v>/ 下 AI 复制进去的可双击发布脚本副本（非标准件，
# 标准件永远在 tools/publish-release-direct.ps1）。忽略副本，防误提交。
temp/**/publish-release.ps1

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

dist/config
dist/test