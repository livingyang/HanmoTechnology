# 2026-09-05

## 公司主页重构方案

需求：把公开仓库 `github.com/livingyang/HanmoTechnology` 从"Fab 素材目录"改造成公司主页，保留 GitHub Pages。

Pages 配置现状：Deploy from a branch → `main` / `/docs`，默认域 `livingyang.github.io/HanmoTechnology`，HTTPS 已开。`docs/` 下无 `index.html`，主页实际是空白。

## 已确认决策

- 建站技术：**Vue 3 + Vite + TypeScript**，GitHub Actions 部署
- Fab 素材：直接删除 / 挪到本地（保留备份后再从仓库移除）
- 域名：保持默认二级域名，不绑自定义域
- 本轮执行限度：先出方案不动仓库

## 目标目录

```
.github/workflows/pages.yml     # 打包 + 部署
website/                        # Vue3+Vite+TS 主页源码（base: /HanmoTechnology/）
tools/                          # 保留 Fab 打包脚本（不碰）
docs/                           # 清空 / 移除发布路径
```

## 关键坑位（执行时检查）

- vite.config.ts 的 `base: '/HanmoTechnology/'`，子路径部署的常见翻车点
- 不动 `tools/` 业务脚本
- 公开仓库，页面内容避免敏感信息

## 资料更新

- 新建 `COMPANY.md` 作为构建主页的权威数据源
- 公司中文全称：深圳市汉末科技有限公司
- 公司英文全称：Shenzhen Hanmo Technology Co., Ltd.
- 简称：汉末科技 / Hanmo Technology
- 备注：注意是"汉末"（Late Han）不是"汉默"，后续主页文案不要写错

## 主页板块（执行时填充）

1. Hero — 深圳市汉末科技有限公司 · 专注 UE5 游戏与工具链
2. 关于我们 — 团队定位 / 技术栈（UE 5.8 + Puerts + Vue 3）
3. 产品 — Fab 商店在售插件：SmartNavigationWidget
4. 项目 — HanmoSekiro 类魂动作游戏
5. 联系方式 — GitHub / Fab / lhm.cold@163.com

## 下一步（待用户说"开始"）

1. 备份 `docs/SmartNavigationWidget/` 到本地 → 从仓库删除
2. 新建 `website/`（Vite + Vue3 + TS），配 base
3. 写六个 section 的中文内容
4. 加 `.github/workflows/pages.yml`
5. 本地 `npm run build` 验证
6. 用户手动把 Pages Source 切到 GitHub Actions 并 push
7. 访问验证

## 经验沉淀

仓库重构方案模板：现状诊断（Pages 配置 + 内容）→ 关键决策（技术/资料/域名/执行限度）→ 目标目录结构 → 部署链路 → 内容板块 → 坑位 → 步骤清单。本轮沉淀完毕。

---

## Web 试玩产品托管规范（PLAY-INTEGRATION）

新需求：托管多个 Web 试玩产品（HanmoIdleMMO 为首个），给出统一接入规范。

架构：Hub (HanmoTechnology) 只做注册表 + 列表页，每个产品独立仓库 + 独立 GitHub Pages，Hub 不耦合产品内部实现，通过 `manifest.json` + 注册表 `products.json` 解耦。

新增文件：
- `HanmoTechnology/PLAY-INTEGRATION.md`（v1.0 规范）
- `HanmoTechnology/products.json`（注册表，已含 HanmoIdleMMO 一项）
- `HanmoIdleMMO/manifest.json`（产品自描述）
- `HanmoIdleMMO/.github/workflows/pages.yml`（Actions 部署模板）
- `HanmoIdleMMO/thumbnail.svg`（占位缩略图，后续可换 PNG）

规范要点：
- 仓库命名 `Hanmo<Xxx>`，产品自带 Pages 在 `livingyang.github.io/<repo>/`
- 必填：`manifest.json`（schemaVersion 1.0）、`thumbnail`、Actions workflow
- 构建产物目录必须含 `index.html`，`base` 推荐 `'./'`
- 注册表 `products.json` 人工维护，Hub 按 `order` 排序
- 状态机：alpha → beta → released → archived
- iframe 嵌入需 `embeddable: true`，Hub 与产品同源（`*.github.io`）

待用户执行（部署 HanmoIdleMMO）：
1. push 三个新文件到 HanmoIdleMMO 仓库
2. 仓库 Settings → Pages → Source 切到 GitHub Actions
3. 等 Actions 跑通
4. 访问 https://livingyang.github.io/HanmoIdleMMO/ 验证

## 架构调整：Hub 集中托管（v2.0 覆盖 v1.0）

用户新决定：试玩产品的仓库本身不提供 Page 服务，dist 生成物集中放到当前项目（HanmoTechnology）。

v1.0 → v2.0 主要差异：
- ❌ 产品仓库自带的 `pages.yml` 部署流水线（已删除）
- ✅ 产品仓库改用 `package.yml`，只产出 `Hanmo-<slug>-<version>-web.zip` artifact
- ✅ Hub 新增 `products/<slug>/<version>/` 集中托管目录
- ✅ Hub 新增 `inbox/` 收件箱 + ingest 工作流（手动 workflow_dispatch）
- ✅ Hub `tools/ingest-product.ts` 校验、解压、原子写产物、更新注册表
- ✅ Hub `pages.yml` 合并 `website/dist/` + `products/` 一起部署
- ✅ 注册表 `products.json` 改用精确 entry 路径，废弃 v1.0 的 entryUrl/manifest 自发现
- ✅ `manifest.json` schemaVersion 升级到 2.0（新增 `homepageUrl` 字段）

新增/修改文件清单：
- `HanmoTechnology/PLAY-INTEGRATION.md`（v1.0 → v2.0，覆盖）
- `HanmoTechnology/.github/workflows/pages.yml`（新增：主页+产物合并部署）
- `HanmoTechnology/.github/workflows/ingest-product.yml`（新增：手动 ingest）
- `HanmoTechnology/tools/ingest-product.ts`（新增：ingest 脚本）
- `HanmoTechnology/products/.gitkeep`（新增）
- `HanmoTechnology/inbox/.gitkeep`（新增）
- `HanmoTechnology/.gitattributes`（新增：products/ 标记为 vendored + LF）
- `HanmoTechnology/products.json`（更新：用 entry 路径）
- `HanmoIdleMMO/manifest.json`（更新：schemaVersion 2.0）
- `HanmoIdleMMO/.github/workflows/pages.yml`（删除：产品不再部署 Pages）
- `HanmoIdleMMO/.github/workflows/package.yml`（新增：只打 zip artifact）

部署链路：
1. 产品仓库 push → Actions 出 zip
2. 开发者把 zip 放进 Hub 的 `inbox/`
3. Hub 触发 Ingest product workflow → 解压到 `products/<slug>/<version>/` + 更新 `products.json`
4. Hub pages.yml 自动触发 → 合并主页 + 产物 → 部署

待用户执行（HanmoIdleMMO v2.0 部署）：
1. push 修改后的 `manifest.json`（schemaVersion=2.0）+ `package.yml` + `thumbnail.svg` 到 HanmoIdleMMO
2. push 修改后的 `PLAY-INTEGRATION.md`、`products.json`、`.gitattributes`、新增的工作流与脚本到 HanmoTechnology
3. HanmoIdleMMO Actions 跑通 → 下载 zip → 放进 HanmoTechnology 的 `inbox/`
4. 触发 HanmoTechnology 的 Ingest product workflow → 校验 → 提交
5. 触发 pages.yml → 验证 https://livingyang.github.io/HanmoTechnology/products/idle-mmo/0.0.2/

---

## 架构再调整：demos 直挂（v3.0，待确认）

用户追问：为什么要产 zip？zip 中转只是冗余步骤。产品仓构建出 dist 后直接同步到 Hub 的 `demos/<slug>/` 目录即可，访问路径 `{{Hub Url}}/demos/<slug>/index.html`。

### v2.0 → v3.0 主要差异

- ❌ v2.0 zip 中转 + ingest 脚本 + inbox/ 目录（整条链路作废）
- ✅ 产品仓工作流改为：build → checkout Hub → rsync dist-web → commit & push（用 PAT）
- ✅ Hub 把 `products/` 改名为 `demos/`，slug 单层不再带 `<version>/`
- ✅ Hub `pages.yml`：构建 website + demos/ 原样保留 + 一次性 Pages 部署
- ✅ 注册表 `products.json`：`entry` 字段从 `products/<slug>/<version>/` 改成 `demos/<slug>/`；`version` 字段降级为可选
- ✅ Manifest 校验改在产品仓工作流 push 前自检 + Hub pages.yml 部署前校验（不再依赖解压后脚本）
- ✅ 体积策略保留：>200MB 工作流 fail；不依赖 zip 大小判断

### v3.0 待办清单（用户确认后执行）

- [ ] 重写 `PLAY-INTEGRATION.md` 为 v3.0
- [ ] 删除 `HanmoTechnology/inbox/`、`tools/ingest-product.ts`、`.github/workflows/ingest-product.yml`
- [ ] 改名：`HanmoTechnology/products/` → `demos/`
- [ ] 更新 `HanmoTechnology/products.json`：路径字段、删除 inbox 相关说明
- [ ] 更新 `HanmoTechnology/.github/workflows/pages.yml`：去掉"复制 products/" 步骤
- [ ] 重写 `HanmoIdleMMO/.github/workflows/package.yml`：build → checkout Hub → rsync → commit & push
- [ ] 更新 `HanmoIdleMMO/manifest.json`：schemaVersion 升 3.0（可选，待用户确认是否保留 schemaVersion 还是直接去掉）
- [ ] Hub 仓库新增 Secret: `HUB_REPO_TOKEN`（PAT，repo:write 权限）

### v3.0 落地执行情况（已完成）

✅ **已落地**：

Hub 侧（HanmoTechnology）：
- `PLAY-INTEGRATION.md` 删除，新文档改名 `DEMO-HOSTING.md`（更直接）覆盖 v3.0 内容
- `inbox/` 删除（连 .gitkeep），`tools/ingest-product.ts` 删除，`.github/workflows/ingest-product.yml` 删除
- `products/.gitkeep` 删除，新建 `demos/.gitkeep`
- `products.json` 重写：entry 路径 `demos/HanmoIdleMMO/`，与新 demos 目录对齐
- `.gitattributes` 重写：products/** → demos/**
- `.github/workflows/pages.yml` 重写：构建 website/（如存在）+ 复制 demos/ + 一次性 Pages 部署；增加 validate-manifests job
- `tools/validate-manifests.mjs` 新建：扫描 demos/*/manifest.json 校验必填字段/路径/版本/缩略图/入口文件存在性；空 demos/ 时静默 pass

产品侧（HanmoIdleMMO）：
- `.github/workflows/package.yml` 删除（旧的 zip 打包）
- `.github/workflows/publish-demo.yml` 新建：完整 v3.0 同步工作流（build + 校验 + 同步到 Hub）
- `manifest.json`：schemaVersion 升级到 3.0，移除 homepageUrl（v3.0 用相对路径，不再需要绝对 URL）

验证脚本自检通过：
- `demos/` 空 → `[skip] demos/ is empty` exit 0
- 模拟有 demo 但缺 index.html → `[ERR] entry 文件不存在` exit 1（按设计阻塞）

### 用户表达的工作偏好（值得记住）

- 偏好轻量、链路短的方案，对"过度工程化"敏感
- "为什么不做 X" 这种反问要直接认可并给替代方案，不要为被推翻的方案找借口
- 文档先行确认 → 文件落地两步走，本轮确认再动手
- 接入文档要让其他开发者能"3 件事 + 1 段 yaml"上手，不需要懂 Hub 内部

### 待用户手动执行（v3.0 部署）

1. 在 Hub 仓库 Settings → Secrets → 新增 `HUB_REPO_TOKEN`（GitHub PAT，权限 `repo` write）
2. 把 HanmoIdleMMO 这边的更新 push：
   - 新 `manifest.json`（schemaVersion=3.0）
   - 新 `publish-demo.yml`
   - 删 `package.yml`（或保留也行，但不再被触发）
3. 把 HanmoTechnology 这边的更新 push：
   - 新 `DEMO-HOSTING.md`
   - 新 `products.json`
   - 新 `demos/.gitkeep`
   - 新 `.gitattributes`
   - 新 `pages.yml`
   - 新 `validate-manifests.mjs`
4. Hub 仓库 Settings → Pages → Source 切到 "GitHub Actions"（之前是 "Deploy from a branch"）
5. push Hub 触发 pages.yml → 跑通（首次因为 demos/ 空会 skip 校验）
6. push HanmoIdleMMO 触发 publish-demo.yml → 自动把 dist 同步到 Hub 的 `demos/HanmoIdleMMO/`（需要第 1 步的 secret）
7. Hub pages.yml 自动再触发 → 验证 `https://livingyang.github.io/HanmoTechnology/demos/HanmoIdleMMO/`

---

## website/ 主页工程搭建（已完成，22 个文件）

Vue 3 + Vite 6 + TypeScript 5.6 栈，符合用户"贴现有 Vue3/TS 技术栈"的偏好。`base: '/HanmoTechnology/'` 已在 `vite.config.ts` 设好（GitHub Pages 子路径部署的关键坑）。

文件结构：
```
website/
├── package.json              # Vue 3.5 / Vite 6 / TS 5.6
├── vite.config.ts            # base: '/HanmoTechnology/'
├── tsconfig.json + tsconfig.node.json
├── index.html                # 中文 lang / theme-color / favicon 都设了
├── env.d.ts
├── public/favicon.svg        # 内联 SVG logo
└── src/
    ├── main.ts
    ├── App.vue               # 整体布局 + NavBar + Sections + Footer
    ├── style.css             # 深色主题 + CSS 变量 + 全部组件样式
    ├── data/products.ts      # 静态 import 仓根 products.json
    ├── components/
    │   ├── NavBar.vue        # 顶部导航 + scroll-spy
    │   ├── SiteFooter.vue
    │   ├── SectionTitle.vue  # 通用 section 容器
    │   ├── ProductCard.vue   # 通用卡片（products / projects 板块）
    │   └── DemoCard.vue      # 试玩卡片：缩略图 + ▶ 直接试玩 iframe + 新窗口 + 源码
    └── sections/
        ├── HeroSection.vue   # 公司全称 / 一句话定位 / 三个 CTA
        ├── AboutSection.vue  # 关于 + 技术栈卡片
        ├── ProductsSection.vue  # Fab 上架的 SmartNavigationWidget + Puerts Web Bridge
        ├── ProjectsSection.vue  # HanmoSekiro / HanmoIdleMMO
        ├── PlaySection.vue   # ★ 从 products.json 拉 demo 卡片渲染
        └── ContactSection.vue   # 邮箱 / GitHub / Hub URL / 位置
```

关键技术点：
- `data/products.ts` 用 `import productsJson from '../../../products.json'` 静态嵌入，构建时数据已固化在 bundle，**无需运行时 fetch**，首屏稳定、零 hydration 抖动
- DemoCard 的 iframe 嵌入按 v3.0 规范：`src = ${BASE_URL}demos/<slug>/`、sandbox 取 manifest.sandbox、loading="lazy"
- iframe 与 Hub 同主机 `*.github.io`，**零跨域问题**
- 缩略图懒加载：默认显示 `<img>`，点击"▶ 直接试玩"才挂 iframe（保护首屏带宽）
- 缩略图加载失败兜底：显示 `HanmoIdleMMO · thumbnail.svg` 文字标签
- PlaySection 空状态兜底：`products.length === 0` 时显示"暂无试玩产品"

未跑 `npm install`（避免占用系统资源），让用户自己装。首次构建命令：
```bash
cd website
npm install
npm run build
```

发布到 Hub 的 `pages.yml` 已经在 v3.0 落地时写好（构建 website/ 复制 demos/ 一次性部署），这次搭好骨架后整条链路就齐了。

---

## 产品仓零侵入原则（最终设计）

用户明确需求："如果工作环境切换到 HanmoIdleMMO，把发布文档发给 HanmoIdleMMO 的 AI，就能知道怎么把构建产物生成到 HanmoTechnology"。

设计调整：
- ❌ 删除 `HanmoIdleMMO/manifest.json`
- ❌ 删除 `HanmoIdleMMO/thumbnail.svg`
- ❌ 删除 `HanmoIdleMMO/.github/workflows/publish-demo.yml`
- ❌ 删除 `HanmoIdleMMO/.github/workflows/` 与 `.github/` 空目录
- ✅ 新增 `HanmoTechnology/scripts/publish-demo.sh`（231 行 bash 脚本，零依赖、颜色输出、dry-run 模式）
- ✅ 新增 `HanmoTechnology/PUBLISH-TO-HUB.md`（给 AI agent 的 5 分钟上手指南，自包含完整命令）
- ✅ 重写 `HanmoTechnology/DEMO-HOSTING.md`（强调"产品仓零侵入"，manifest schema 由脚本生成）

publish-demo.sh 关键能力：
- 校验必填/产物/体积（≤ 200MB）
- 自动生成 manifest.json（schemaVersion 3.0，写入 demos/<slug>/manifest.json）
- clone Hub（注入 HUB_REPO_TOKEN） → 拷到 demos/<slug>/
- 自动更新 Hub 的 products.json（Node 安全合并 JSON；缺 node 跳过并 warn）
- commit & push；token 自动从 push 输出脱敏
- 颜色输出、--dry-run、-v 详细日志
- 退出码明确（0/1/2/3/4 对应成功/参数/校验/网络/git）

入口 URL（AI agent 直接 fetch）：
- https://raw.githubusercontent.com/livingyang/HanmoTechnology/main/scripts/publish-demo.sh
- https://raw.githubusercontent.com/livingyang/HanmoTechnology/main/PUBLISH-TO-HUB.md

预期工作流：用户切到 HanmoIdleMMO 仓 → 复制 PUBLISH-TO-HUB.md 给 AI agent → AI 按文档调脚本 → Hub 自动部署。

### 用户工作偏好沉淀

- **产品仓零侵入**：AI agent 跨仓协作场景下，不要给产品仓下发任何文件
- **文档自包含**：AI agent 拿到文档不需要再查别的资料就能上手
- **脚本可独立运行**：AI agent 可以 curl 远程脚本 + bash 执行，不需要本地克隆 Hub

---

## v3.0 进一步收敛：去掉多版本状态机

用户补充：独立开发者场景下所有 demo 仅展示个人能力，不需要 alpha/beta/released 多版本并存。

调整：
- `DEMO-HOSTING.md` 第 6 节"状态机" → 改为"demo 标签约定"，明确 demo 统一填 `alpha`
- 附录 A `status` 字段默认值标 `alpha`，其他值仅供未来扩展
- 顶部导语加"场景定位"：独立开发者工作室
- 长期 MEMORY.md 写入"独立开发者 demo 场景"业务说明
- 用户偏好记忆："做轻量、避免过度工程化"，接入文档要"3 件事 + 1 段 yaml"

---

## HanmoIdleMMO 首次接入 Review（产品端 AI 跨仓发布后）

用户贴产品端 AI 的发布记录截图，让我 review 是否异常 + 发布后对 Hub 的影响。

**产品端 AI 操作核实（全部合规，未越权）**：
- §4.1 build:web → 530K（产物实际 542K）✅
- §4.2 cp 到 `demos/HanmoIdleMMO/` ✅ 产物完整（index.html + assets + manifest.json + thumbnail.svg + placeholders/）
- §4.3 products.json 更新：version 0.0.2→0.0.1（与 package.json 对齐，✅ package.json 确认为 0.0.1）；新增 homepage/sandbox；移除 note ✅
- manifest.json 补 `entry`/`thumbnail` 字段 → validate-manifests.mjs 必填 ✅ 合规
- git status：产品端 AI 只改了 products.json + 加了 demos/，**没动 website/ 和 tools/，没动 DEMO-HOSTING.md** → 严格守"产品仓零侵入 + Hub 只改注册表"边界 ✅
- 产品端 AI 指出 2 处不一致但**没擅改**（最小改动原则）✅

**真正的问题（P0/P1，都是 Hub 自己埋的，非产品端 AI 错）**：

1. **P0 · products.json 裸数组 vs products.ts 期望包装格式**：
   - `products.json` 顶层是裸数组 `[{...}]`，但 `website/src/data/products.ts` 第51行 `(productsJson as ProductsRegistry).products.slice()` —— productsJson 是数组，`.products`=undefined，`.slice()` 直接 💥 TypeError（build/运行时崩溃）。
   - **根源**：v3.0 我写 products.json 时用了裸数组，但 products.ts 代码 + DEMO-HOSTING.md §4.3 规范都期望 `{schemaVersion, products:[...]}`。数据/代码/规范三方矛盾。
   - node 验证：`products.slice() => 💥 Cannot read properties of undefined (reading 'slice')`。

2. **P1 · products.json thumbnail 完整路径 vs demoThumbSrc 按文件名拼**：
   - `demoThumbSrc(slug, thumbnail)` 拼 `${BASE_URL}demos/${slug}/${thumbnail}`，但 products.json 的 thumbnail 是完整路径 `demos/HanmoIdleMMO/thumbnail.svg` → 最终 `demos/HanmoIdleMMO/demos/HanmoIdleMMO/thumbnail.svg` → 404。
   - **根源**：规范 §4.3 用"完整路径"模型（entry/thumbnail 都是 `demos/<slug>/...`），但 website 的 demoSrc/demoThumbSrc 用"拼 slug + 文件名"模型。两者矛盾。
   - node 验证：拼接结果含 `demos/HanmoIdleMMO/demos/HanmoIdleMMO/` → ❌ 404 → fallback 显示 "Hanmo Idle MMO · demos/HanmoIdleMMO/thumbnail.svg" 文本。

**修复方向（需用户拍板）**：
- P0：改 products.json 为包装格式 `{schemaVersion, products:[...]}`，对齐规范§4.3 + products.ts。**或**改 products.ts 兼容裸数组（后者破坏规范契约，不推荐）。
- P1：统一成"规范完整路径模型"——products.json entry/thumbnail 存 `demos/<slug>/` 完整路径，website 的 demoSrc/demoThumbSrc 改成直接用完整路径 `${BASE_URL}${entry}` / `${BASE_URL}${thumbnail}`（不再拼 slug）。**或**改 products.json 的 thumbnail 存纯文件名（后者与规范§4.3 冲突）。

**经验沉淀**：
- 写 products.json 时务必与 website 的解析代码 + 规范示例三方对齐，别自造数据模型
- 规范示例和实际数据文件必须一致，否则外部 AI 按规范写会踩坑；产品端 AI 已严谨指出但不该由它来修 Hub 代码

**用户已拍板修复方向**（P0: 改 products.json 包装格式 / P1: 统一完整路径模型 / 两处一起修）：

已执行修复（4 个文件）：
1. `products.json`：裸数组 → `{schemaVersion:"3.0", products:[...]}` 包装格式（保留产品端 AI 更新的字段：version 0.0.1 / homepage / sandbox）
2. `website/src/data/products.ts`：`demoSrc(entry)` = `${BASE_URL}${entry}`、`demoThumbSrc(thumbnail)` = `${BASE_URL}${thumbnail}`（不再拼 slug）；接口注释改"完整路径"语义；ProductEntry 补 `addedAt?`/`updatedAt?`
3. `website/src/components/DemoCard.vue`：调用处改 `demoThumbSrc(props.product.thumbnail)` / `demoSrc(props.product.entry)`
4. `DEMO-HOSTING.md` §4.3：顶层 `version:"1.0"` → `schemaVersion:"3.0"`；字段说明明确 entry/thumbnail 为"相对 Hub 根的完整路径"，并警示不要只填文件名（否则 404）

验证结果：
- node 逻辑验证：`.products` 存在、`.slice()` 正常 ✅；thumbnail/entry 拼接不再重复 ✅
- `node tools/validate-manifests.mjs`：`✓ 全部通过` exit=0（demos/HanmoIdleMMO 合规）
- tsconfig.json：resolveJsonModule:true + include 含 products.json ✅

**遗留**：未跑 `npm run build`（避免占资源，用户偏好）；push 前用户需在 website/ 跑一次 `npm install && npm run build` 确认 vue-tsc + vite 通过。

---



## v3.1：彻底去掉自动同步脚本，回归手动 cp + push

用户再次调整："不需要 publish-demo，只需要产品把生成物更新到 HanmoTechnology，可能有好几个产品都更新了生成物，然后我会手动更新到 github 仓库，利用 pages 的自动更新能力进行更新显示版本"。

核心变化：**全部手动**。不是 AI agent 在产品仓调脚本，也不是 Hub 自动 ingest，就是 Hub owner 自己手动 cp + 改 products.json + git push。

调整：
- ❌ 删除 `HanmoTechnology/scripts/publish-demo.sh`（连同 `scripts/` 目录）
- ❌ 删除 `HanmoTechnology/PUBLISH-TO-HUB.md`（不再需要 AI agent 文档）
- ✅ 保留 `website/`（用户上一轮"开始搭建"的产物，要"更新显示版本"必须有主页渲染）
- ✅ 保留 `tools/validate-manifests.mjs`（pages.yml 部署前兜底校验）
- ✅ 保留 `.github/workflows/pages.yml`（用户要的"pages 自动更新能力"）
- ✅ 重写 `DEMO-HOSTING.md` 为 v3.1：
  - 顶部"工作流一句话"：**产品仓本地 build → 手动 cp 到 Hub demos/ → 手动更新 products.json → 手动 git push → Pages 自动重新部署**
  - 删掉所有"AI agent 在产品仓调脚本"章节
  - §4 一次性发布流程（4.1 build / 4.2 cp / 4.3 products.json / 4.4 push）
  - §7 故障排查表
  - §8 明确"不需要做的事"（再次防止后人添加自动同步脚本）
  - §10 目录速查

### 工作流最终形态

```
[各产品仓 HanmoXxx]                            [Hub 仓 HanmoTechnology]
                                              
npm run build:web                              rm -rf demos/HanmoIdleMMO
       │                                       cp -r ../HanmoIdleMMO/dist-web \
       ▼                                       demos/HanmoIdleMMO
dist-web/                                       │
  ├── index.html                                ▼
  ├── manifest.json                      编辑 products.json 加条目
  └── thumbnail.svg                             │
                                               ▼
                                       git add + commit + push
                                               │
                                               ▼
                                    GitHub Actions pages.yml
                                    （校验 manifests + 构建 website + 上传 Pages）
                                               │
                                               ▼
                          https://livingyang.github.io/HanmoTechnology/
```

### 用户偏好（再次强化）

- **手动优于自动**：宁可多写几行 git 命令，也不想运维任何自动化管线
- **可读优于优雅**：手动操作透明可控，自动化反而是黑盒
- **零侵入是底线**：所有"接入"动作收敛在 Hub；产品仓连 manifest.json 都不需要

### 待用户手动执行

1. Hub 仓 Settings → Pages → Source 切到 **GitHub Actions**
2. Hub 仓本地 `cd website && npm install`（第一次才需要）
3. 各产品仓 build 出 `dist-web/`
4. Hub 仓手动 cp + 编辑 products.json + git push
5. 等 Actions → 验证 `https://livingyang.github.io/HanmoTechnology/demos/<slug>/`

---

## v3.1 场景细化：AI 在产品仓跨仓发布

用户再次确认需求："工作空间切换到 HanmoIdleMMO，告诉 AI 按 DEMO-HOSTING 发布即可"。两个核心细节对齐：

| 决策 | 结果 |
|---|---|
| AI 怎么拿到 DEMO-HOSTING.md | **本地双仓都在机器上**，用户告诉 AI Hub 仓本地路径，AI 跨仓读文档 + 跨仓操作 |
| AI 负责哪些步骤 | **AI 做 build + cp + 编辑 products.json，你手动 git push** |

AI 工作范围（产品仓工作空间下）：
- [AI·产品仓] `npm install` + `npm run build:web` → `dist-web/`
- [AI·Hub仓] `cd $HUB && rm -rf demos/<slug> && cp -r <产品仓>/dist-web demos/<slug>/`
- [AI·Hub仓] 编辑 `products.json`（追加/更新条目）
- [你·Hub仓] `node tools/validate-manifests.mjs` 校验 + `git add + commit + push`

AI 不需要 Hub 仓 git push 权限。

### DEMO-HOSTING.md v3.1 微调

- §1 工作流一句话改成"AI 跨仓 cp + 编辑 products.json，你手动 push"
- §4 每个子步骤加了 `[AI·产品仓]` / `[AI·Hub仓]` / `[你·Hub仓]` 标签
- §6 接入清单每条加 [谁做] 标签
- §8 不需要做的事新增"AI 不需要 git push 权限"

### 长期 MEMORY.md 同步

Web 试玩产品托管 v3.1 段描述从"全部手动"细化为"AI 在产品仓做 build+cp+products.json，push 由 Hub owner 手动"。

### 真实用户工作流（完整闭环）

1. 用户切工作空间到 HanmoIdleMMO（产品仓）
2. 用户告诉 AI："按 `C:\developer\hanmo\HanmoTechnology\DEMO-HOSTING.md` §4.1~§4.3 发布到 Hub 仓（路径 `C:\developer\hanmo\HanmoTechnology`）"
3. AI build → cp → 编辑 products.json
4. AI 报告完成，列出修改的文件清单
5. 用户切到 Hub 仓（本地路径已开）→ `git status` 确认 → `git add + commit + push`
6. GitHub Actions 自动跑 pages.yml → Pages 重新部署
7. 访问 `https://livingyang.github.io/HanmoTechnology/demos/<slug>/` 验证

### 用户偏好（再强化）

- 工作空间切换是真实操作：用户会切到产品仓，AI 在产品仓工作空间下完成能做的所有事
- 关键节点（git push）留给用户控制，原因可能是想亲眼看 commit diff 或对安全敏感

---

## 真实跑 npm run build + commit（2026-09-05 21:36~21:42）

用户主动要求："由你执行 npm run build 并且 commit"——明确授权执行，不算主动发起大型构建。

### 实际跑了什么

1. `npm install`（51 包，1m35s）
2. `npm run build` 跑了 3 次：
   - 第 1 次：TS2339 `import.meta.env` 不存在（**根因：tsconfig include 没列 env.d.ts**，vite/client 类型未注入）→ **改 `website/tsconfig.json` include 加 `"env.d.ts"`**
   - 第 2 次：rollup `Could not resolve "../../../public/favicon.svg"`（**根因：NavBar.vue 在 src/components/，3 级 `../` 多 1 级**）→ **改用 `import.meta.env.BASE_URL + 'favicon.svg'`**（Vite 推荐写法，public 资源自动复制到 dist 根）
   - 第 3 次：✅ 通过，760ms 产出 `dist/index.html 0.81kB` + `assets/index-*.js 76kB` + `assets/index-*.css 8.5kB`

### 还修了 2 个 gitignore 问题

- `website/.gitignore` 不存在 → `website/dist/`、`*.tsbuildinfo` 会被误 add
- 解决：建 `website/.gitignore`（忽略 `dist/`、`*.tsbuildinfo`、`.vite/`），但 **不忽略 `package-lock.json`**（pages.yml 用 `npm ci` 必须 lockfile）

### Commit 策略：拆 2 个

1. **`212f5df` 修复 website 构建 + 数据模型对齐**（9 文件 1650 行）
   - 全部代码修复 + 数据模型修复 + tsconfig + NavBar + products.json + .gitignore + lockfile + memory
2. **`7ae5497` 首次提交 HanmoIdleMMO Web 试玩（v0.0.1）**（39 文件 542KB）
   - demos/HanmoIdleMMO 整目录（产品端 AI 跨仓 cp 过来的产物）

### 一个坑：commit message 的 `\n` 被字面写入

第一次用 `git commit -m "line1\nline2"` 时 bash 双引号没把 `\n` 转成真换行，结果 git message 写入了字面字符串 `\n`。

修复流程：
1. `git reset --soft HEAD~1` 把 demos 从 commit 2 暂存出来
2. `git reset HEAD` 把 demos 取消 staged（避免 amend 时被吞进 commit 1）
3. 写干净 message 到 `.git/COMMIT_EDITMSG.tmp`
4. `git commit --amend -F .git/COMMIT_EDITMSG.tmp` 改 commit 1 message
5. 重新 `git add demos/` + `git commit -F .git/COMMIT_EDITMSG2.tmp`

**经验**：以后写多行 commit message，**永远用 `git commit -F file` + here-doc 文件**，不要在 `git commit -m "..."` 双引号里塞 `\n`。Windows + bash 转义陷阱太坑。

### 最终状态（ahead of origin/main by 2，未 push）

- `7ae5497` 首次提交 HanmoIdleMMO Web 试玩（v0.0.1）
- `212f5df` 修复 website 构建 + 数据模型对齐
- 工作树 clean
- 用户手动 `git push origin main` 触发 GitHub Actions 部署

## v4.0 重构：合并到 docs/ 单一发布根（45d696b，未 push）

用户决定去掉 GitHub Actions 工作流，改用 GitHub Pages 内置 Branch 部署 + 本地构建。
理由：Actions YAML 是过度工程化，且与用户"手动优于自动、轻量优于重"的偏好相反。

新拓扑：
```
docs/                  ← 唯一发布根（= Pages Source: main /docs）
├── index.html         ← cp 自 website/dist/
├── assets/            ← cp 自 website/dist/
├── favicon.svg
└── demos/             ← 产品端跨仓 cp 进来
    └── HanmoIdleMMO/
website/               ← 源码 + 构建源（dist/ 不入库）
products.json          ← 主页 /play 板块注册表（路径含 docs/）
tools/validate-manifests.mjs  ← 本地手动跑（commit 前）
```

教训：
1. 维护"中间产物 vs 发布根"两个目录是冗余；所有产物该收口到 docs/
2. website/dist/ 不入库（避免和 docs/ 内容重复），删了源 dist 永远能 npm run build 重建
3. Pages Source 保持 `main /docs` 不动；删了 .github/workflows/pages.yml 后 GitHub 自动用内置 Branch 部署

工作流（用户确认执行过的）：
- 主页调样式：`cd website && npm run dev`（HMR，所见即所得）
- 发布：`cd website && npm run build` → `cp -r website/dist/. ../docs/` → `node ../tools/validate-manifests.mjs` → `git add docs/ + commit + push`
- 产品端接入不变：build:web → cp 到 docs/demos/<slug>/ → products.json 追加项

## Bug 修正：products.json 路径带 docs/ 前缀导致 404（45d696b 后）

问题：把 products.json 的 entry/thumbnail 改成了 `docs/demos/HanmoIdleMMO/`，但主页
demoSrc = `${BASE_URL}${entry}`，BASE_URL=`/HanmoTechnology/`，拼出
`/HanmoTechnology/docs/demos/HanmoIdleMMO/` → 404。

根因：Pages 源是 `main /docs` 时，GitHub 把 docs/ 当站点根，URL 里不含 /docs。
物理在 docs/demos/，但 URL 路径必须从 demos/ 开始。products.json 的 entry/thumbnail
是 **URL 路径**（不含 docs/ 前缀），不是物理文件路径。

修正：
- products.json entry/thumbnail → 回到 `demos/HanmoIdleMMO/`（去 docs 前缀）
- DEMO-HOSTING.md §4.3 示例 + 字段说明同步，并加 ⚠️ 警告
- 重新 npm run build（products.json 是构建时静态 import，必须重 build）
- 删旧产物 docs/assets/index-CVn57SxX.js，保留新的 index-cm6JLepm.js

教训：
1. products.json 存的是 URL 路径，不是物理路径——docs/ 前缀永远不该进 products.json
2. validate-manifests.mjs 只扫 docs/demos/*/manifest.json，**不校验 products.json**，
   所以它会"通过"但主页链接错——products.json 路径要靠人核对
3. manifest.json 里的字段才是物理相对路径（在 docs/demos/<slug>/ 下），
   products.json 里的 entry/thumbnail 是 URL 路径——两者语义不同，别混

## 品牌设计图落地：favicon + 导航栏 logo（8499b91，未 push）

用户提供三个品牌设计图（website/public/design/）：
- hanmo-mark.png  2160x2160 透明底，深色六边形边框 + 蓝色渐变 H 标志（品牌图标）
- hanmo-lockup.png 2960x840  完整 logo + 文字 + slogan 组合
- hanmo-colors.png 2600x1120 BRAND COLOR 色板（NIGHTSHADE #0A0E1A / CORE BLUE
  #3A6FC0 / PRISM BLUE #5EA0EC / FROST CYAN #8EF2FF / MIST SLATE #6F87B8）

favicon 替换（用手绘 favicon.svg → mark）：
- 生成 favicon.ico(16/32/48) + favicon-32.png + favicon-48.png + apple-touch-icon(180)
  → 用 Pillow，深色圆角底(#0A0E1A)叠加 mark，保证任何背景标签页/桌面图标清晰
- 导航栏 logo 换成透明 mark → logo.png(256px)，深色导航栏上直接显六边形 H
- theme-color 从 #0e1116 调成 #0a0e16(NIGHTSHADE)
- index.html favicon 引用 + NavBar logo 路径同步
- 删除不再引用的旧 favicon.svg（源 + 产物）

技术点：
- P 模式 PNG 用 PIL convert('RGBA') 再处理，四角 alpha=0 确认透明底
- 导航栏用透明 logo，favicon 用深底组合——两者用途不同，透明度策略不同
- public/ 是 Vite 静态目录，build 时全量拷到 dist/，三个设计图也随之发布

结论：品牌色已被粉丝站采用（六边形 H + 渐变蓝 + NIGHTSHADE 深底）。
